# [ Box - API ]
API en NestJS encargar procesar y almacenar los datos y archivos del aplicativo

# Variables de entorno
```env
PORT=3030
NODE_ENV=development

APP_DIR_FILES=../files
APP_URL_MEDIA=url

DB_HOSTNAME=PostgreSQL
DB_HOSTNAME=localhost
DB_USERNAME=postgres
DB_PASSWORD=admin
DB_DATABASE=name_database
DB_PORT=5432
```